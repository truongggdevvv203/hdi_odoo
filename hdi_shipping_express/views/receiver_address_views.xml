<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- Receiver Address List View -->
  <record id="view_receiver_address_list" model="ir.ui.view">
    <field name="name">receiver.address.list</field>
    <field name="model">receiver.address</field>
    <field name="arch" type="xml">
      <list>
        <field name="name"/>
        <field name="phone"/>
        <field name="city"/>
        <field name="active"/>
      </list>
    </field>
  </record>

  <!-- Receiver Address Form View -->
  <record id="view_receiver_address_form" model="ir.ui.view">
    <field name="name">receiver.address.form</field>
    <field name="model">receiver.address</field>
    <field name="arch" type="xml">
      <form>
        <sheet>
          <group string="Thông tin cơ bản" name="basic_group">
            <field name="name"/>
            <field name="phone"/>
            <field name="email"/>
          </group>

          <group string="Địa chỉ" name="address_group">
            <field name="street"/>
            <field name="street2"/>
            <field name="city"/>
            <field name="state_id"/>
            <field name="zip"/>
            <field name="country_id"/>
          </group>

          <group string="Cài đặt" name="settings_group">
            <field name="active"/>
          </group>
        </sheet>
      </form>
    </field>
  </record>

  <!-- Receiver Address Action -->
  <record id="action_receiver_address" model="ir.actions.act_window">
    <field name="name">Địa chỉ nhận hàng</field>
    <field name="res_model">receiver.address</field>
    <field name="view_mode">list,form</field>
  </record>
</odoo>