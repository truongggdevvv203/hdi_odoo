<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- CRM Opportunity Form View -->
        <record id="view_crm_opportunity_form" model="ir.ui.view">
            <field name="name">crm.opportunity.form</field>
            <field name="model">crm.opportunity</field>
            <field name="arch" type="xml">
                <form string="Opportunity">
                    <header>
                        <button name="action_set_won" type="object" string="Mark as Won" class="oe_highlight" />
                        <button name="action_set_lost" type="object" string="Mark as Lost" />
                        <button name="action_create_sale_order" type="object" string="Create Sale Order" />
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <field name="name" placeholder="Opportunity Name"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="partner_id" required="1"/>
                                <field name="stage_id"/>
                                <field name="probability" widget="percentpie"/>
                                <field name="priority"/>
                            </group>
                            <group>
                                <field name="expected_revenue"/>
                                <field name="weighted_revenue"/>
                                <field name="date_deadline"/>
                                <field name="date_closed"/>
                            </group>
                        </group>
                        <group>
                            <group>
                                <field name="user_id"/>
                                <field name="team_id"/>
                                <field name="company_id"/>
                            </group>
                            <group>
                                <field name="lead_id"/>
                                <field name="tag_ids" widget="many2many_tags"/>
                            </group>
                        </group>
                        <field name="description" placeholder="Add internal notes..."/>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids"/>
                        <field name="activity_ids"/>
                        <field name="message_ids"/>
                    </div>
                </form>
            </field>
        </record>

        <!-- CRM Opportunity List View -->
        <record id="view_crm_opportunity_list" model="ir.ui.view">
            <field name="name">crm.opportunity.list</field>
            <field name="model">crm.opportunity</field>
            <field name="arch" type="xml">
                <list string="Opportunities" sample="1">
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="stage_id"/>
                    <field name="probability"/>
                    <field name="expected_revenue" sum="Total Revenue"/>
                    <field name="weighted_revenue" sum="Weighted Revenue"/>
                    <field name="date_deadline"/>
                    <field name="user_id"/>
                </list>
            </field>
        </record>

        <!-- CRM Opportunity Kanban View -->
        <record id="view_crm_opportunity_kanban" model="ir.ui.view">
            <field name="name">crm.opportunity.kanban</field>
            <field name="model">crm.opportunity</field>
            <field name="arch" type="xml">
                <kanban default_group_by="stage_id" sample="1">
                    <field name="id"/>
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="stage_id"/>
                    <field name="probability"/>
                    <field name="expected_revenue"/>
                    <field name="user_id"/>
                    <templates>
                        <t t-name="kanban-box">
                            <div class="oe_kanban_card">
                                <div class="oe_kanban_content">
                                    <strong><field name="name"/></strong>
                                    <div>
                                        <span><field name="partner_id"/></span>
                                    </div>
                                    <div>
                                        <span class="text-muted">Probability: <field name="probability"/>%</span>
                                    </div>
                                    <div class="text-muted">
                                        Revenue: <field name="expected_revenue" widget="monetary"/>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

        <!-- CRM Opportunity Search View -->
        <record id="view_crm_opportunity_search" model="ir.ui.view">
            <field name="name">crm.opportunity.search</field>
            <field name="model">crm.opportunity</field>
            <field name="arch" type="xml">
                <search string="Search Opportunities">
                    <field name="name" string="Opportunity"/>
                    <field name="partner_id"/>
                    <field name="stage_id"/>
                    <field name="user_id"/>
                    <field name="tag_ids"/>
                    <separator/>
                    <filter name="filter_active" string="Active" domain="[('active', '=', True)]" />
                    <filter name="filter_high_probability" string="High Probability" domain="[('probability', '>=', 60)]"/>
                    <filter name="filter_overdue" string="Overdue" domain="[('date_deadline', '&lt;', context_today())]"/>
                    <separator/>
                    <group expand="0" string="Group By">
                        <filter name="group_stage" string="Stage" context="{'group_by': 'stage_id'}"/>
                        <filter name="group_user" string="Assigned to" context="{'group_by': 'user_id'}"/>
                        <filter name="group_partner" string="Customer" context="{'group_by': 'partner_id'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- CRM Opportunity Action -->
        <record id="action_crm_opportunity" model="ir.actions.act_window">
            <field name="name">Opportunities</field>
            <field name="res_model">crm.opportunity</field>
            <field name="view_mode">kanban,list,form</field>
            <field name="view_id" ref="view_crm_opportunity_kanban"/>
            <field name="search_view_id" ref="view_crm_opportunity_search"/>
            <field name="context">{'search_default_filter_active': 1}</field>
        </record>
    </data>
</odoo>
